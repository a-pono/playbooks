# Database credentials
# Copy to .env and fill in real values: cp .env.template .env
# Run `source .env` before executing any queries
# NEVER read .env directly — use env vars in commands

# PostgreSQL
PG_HOST=pg-host
PG_PORT=5432
PG_USER=app_user
PG_PASSWORD=
PG_DATABASE=ecommerce

# ClickHouse
CH_HOST=ch-host
CH_PORT=9000
CH_USER=analyst
CH_PASSWORD=
CH_DATABASE=analytics
